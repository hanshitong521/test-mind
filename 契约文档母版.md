# TestMind 契约文档母版

> **这是什么**：开发方 AI 与测试方 AI 之间的**唯一交接载体**。开发方照本母版的骨架产出一份《项目契约文档》，测试方（TestMind）照它做验收测试。
> **放哪**：`test-mind/契约文档母版.md`（本文件，长期维护）
> **产出物放哪**：各自业务仓根 → `CONTRACT_<模块名>.md`（与代码同仓，跟着代码走）

---

## 一、流转链（谁给谁）

| 步骤 | 谁做 | 输入 | 产出 | 判据 |
|---|---|---|---|---|
| 1 | 玉斌 | 需求描述 / 口述 | 把**本母版**发给开发方 AI | — |
| 2 | 开发方 AI | 本母版 + 需求 + 现有代码 | 《项目契约文档》`CONTRACT_xxx.md` | 走完 §4 骨架全部 10 节（§0–§9），不留空 |
| 3 | 玉斌 | 契约文档 | 转给测试方 AI（TestMind） | — |
| 4 | 测试方 AI | 契约文档（+ 仓库地址） | 验收矩阵执行结果、缺陷清单、回归锚 | `final_gate` = PASS / FAIL / HOLD / BLOCKED |

**契约文档是唯一事实源。** 开发方与测试方看的是同一份，不允许任何一方口头补充或自行解释——两边各说各话是漏测的头号来源。

---

## 二、维护规则（硬要求）

### 2.1 项目契约文档：改功能必须同步改它

只要动了下面任一项，**必须在同一次改动里更新契约文档**，否则契约与代码脱节：

| 动了什么 | 契约文档里必须更新哪节 |
|---|---|
| DB 表结构 / 字段 / CHECK / UNIQUE / 索引 | §2 DDL（**给完整 DDL，不是 diff**） |
| 接口路径 / 方法 / 入参 / 出参 / 错误码 | §3 接口契约 |
| 状态机取值或合法迁移 | §1 术语与状态机 |
| 校验规则、边界值 | §3 请求体约束 + §5 验收矩阵 |
| 依赖的下游行为（超时/失败语义） | §3 副作用说明 + §9 环境信息 |
| 修复了一个缺陷 | §5 验收矩阵加一行 + §7 回归锚登记 |
| 删掉一个接口 / 字段 | §3 标注 `REMOVED`，**不得直接删行**（测试方要知道它曾经存在过） |

**红线**：契约文档更新滞后 → 测试方会按旧契约测 → **假绿**。这是开发方责任，不是测试方漏测。

### 2.2 母版本身：随 TestMind 能力演进更新

本母版是 TestMind 的对外接口约定，遇到以下情况要改它：

- TestMind 新增了可消费的输入类型（新 MCP 工具）
- 发现某类缺陷反复漏测，需要在契约文档里新增必填节
- §五 的 topic 清单需要扩充（当前 10 个）

改完在文末「变更记录」追加一行。

---

## 三、开发方 AI 怎么用

打开本母版 → 照 §4 骨架开一份 `CONTRACT_xxx.md` → 逐节填 → 交回。**不要另起格式。**

### 3.1 新写功能

按 §4 骨架从头填。重点是把**模糊需求冻结成精确口径**——比如"要点库存"必须落成 `qty` 的区间、`stock` 的守卫条件、超发时的期望码。

### 3.2 修改已有功能（差异比全量更重要）

除了填全套骨架，**必须额外补三样**，这是修功能场景独有的：

| 补充项 | 填在哪 | 为什么关键 |
|---|---|---|
| **改动面** | §8 | 给 `base_sha` / `head_sha` 或"改了哪几个文件"；测试方据此确定影响范围，不然只能全量回归 |
| **不得改变的行为** | §7 | 列出**这次改动不许影响**的既有行为，逐条给验证方式。这是回归锚的基底——没有它，测试方不知道"改坏了什么" |
| **影响链** | §6 | 反向检索：谁引用了被改的表/字段/状态值/接口（`文件:行`）。只测改动的方法本身 = 漏测（`SKILL.md:66` 明文禁止） |

**只加功能不填 §7 是最常见的翻车方式**：新功能全绿，旧功能被静默改坏，谁都没发现。

---

## 四、契约文档骨架（照抄这一节）

> 把下面整块复制成 `CONTRACT_xxx.md`，逐节填写。**每节的括号提示是填法说明，填完删掉。**

````markdown
# 契约文档 · <模块/域名>

> 版本：v<N> · 更新：<YYYY-MM-DD> · 对应代码：<base_sha>..<head_sha>
> 变更类型：新功能 / 功能修改 / 缺陷修复

## 0. 需求与冻结口径
<!-- 原始需求原话（可以是模糊的），以及本文把它冻结成了什么精确口径。
     冻结前后的差异就是"歧义被消除"的证据。 -->

## 1. 术语与状态机
<!-- 状态字段的取值表 + 合法迁移图。每个迁移注明触发条件与权限。
     非法迁移的期望响应必须写明（通常是 409）。 -->

| 值 | 含义 |
|---|---|
| 0 | PENDING_PAY |
| 1 | PAID |

合法迁移：
```
状态A --动作--> 状态B   [附加条件/副作用]
```
其余动作/状态一律 <期望码>。

## 2. 数据库 DDL（完整，非 diff）
<!-- 给可直接执行的完整 DDL 原文。CHECK / UNIQUE / NOT NULL / FK 一项都不能省——
     静态预检与 DB 对拍全靠它们。 -->
```sql
CREATE TABLE ...;
```

## 3. 接口契约
<!-- 每个端点一节，复制下面模板： -->

### 3.N `<METHOD> <path>` <一句话用途>
请求体：
```json
{ "字段": "示例值" }
```
约束：<字段级边界，如 qty∈[1,100]；未知字段的处理>
响应码：
- `<2xx>` 成功 → <响应体>；副作用：<写了哪些表、扣了什么>
- `<4xx>` <触发条件> → <响应体>
- **4xx 一律禁止任何写库**，除非下行显式声明例外
- `<5xx>` <触发条件>

## 4. 种子数据
<!-- 造数据方案。每行数据存在的理由必须写明（触发哪条规则），禁随机。 -->

## 5. 验收矩阵 ★核心
<!-- 一行一条规则。这是测试方的执行清单，也是开发方交付前的自检清单。
     涉及并发/幂等/时间/外部依赖的，必须给出"什么条件下会红"。 -->

| # | 规则（业务事实） | 端点 | 期望 | DB 不变量（必须对拍） |
|---|---|---|---|---|
| R1 | | | | |
| R2 | | | | |

## 6. 影响链
<!-- 反向检索：谁引用了本次改动的表/字段/状态值/接口。格式 <文件>:<行>。
     未覆盖的受影响处必须显式处理，禁止静默遗漏。 -->

## 7. 不得改变的行为（回归锚）★修改功能必填
<!-- 这次改动不许影响的既有行为，逐条给验证方式。 -->

| # | 既有行为 | 验证方式 | 期望 |
|---|---|---|---|
| K1 | | | |

## 8. 本次改动面
```json
{
  "base_sha": "",
  "head_sha": "",
  "changed_files": [],
  "endpoints": [
    { "operation_id": "", "method": "", "path": "", "change": "ADDED | MODIFIED | REMOVED" }
  ],
  "risks": {
    "relation": false,
    "idempotency": false,
    "concurrency": false,
    "money": false,
    "state_machine": false,
    "time_expiry": false,
    "dependency": false
  }
}
```
<!-- risks 只打 true 到你认为真会出问题的维度，别全开——全开会稀释注意力。
     打 true 的必须能对应到 §5 里的某条"会红"用例。 -->

## 9. 环境与接入信息
<!-- 测试方怎么跑起来：启动命令、健康检查地址、DB 类型与连接方式、
     下游依赖能不能被代理拦截、是否有可控时钟注入入口。 -->
````

---

## 五、骨架怎么变成 TestMind 的输入

契约文档不是给 TestMind 读的——TestMind 只吃结构化字段。测试方 AI 会把 §4 骨架拆成四类喂进去：

| 契约文档里的节 | → 喂进哪 | 抽到什么 |
|---|---|---|
| §2 DDL | `collect_facts {schema_sql}` | CHECK / UNIQUE / NOT NULL / FK |
| §3 请求体约束 | `collect_facts {openapi}` | required / minimum / maximum / pattern → 逐字段边界与负例 |
| §8 改动面 | `intake_task` | 定改动范围、锁 base..head |
| §5 + §7 中 DDL 表达不出来的 | `add_facts {facts:[{topic,statement,source}]}` | 幂等/状态机/到期/回补/并发等行为规则 |

**可用 topic 清单**（只写这些，其余 DDL/OpenAPI 已覆盖，写了是噪音）：

`oversell` · `idempotency` · `money` · `state` · `time` · `dependency` · `stock:restore` · `scheduler:tick` · `relation:party` · `relation:tenant`

**硬约束**：每条规则必须带 `file:line` 出处。无出处会被拒收——**这是 TestMind 的拒收规则，不是建议**。

---

## 六、可直接粘贴给开发方 AI 的指令

```
这是 TestMind 的契约文档母版。请照它的 §4 骨架，为本次改动产出一份
CONTRACT_<模块名>.md，放到业务仓根目录。

要求：
1. 逐节填满，不留空。填不了的节写"无"并给依据，不要省略整节。
2. §2 的 DDL 给完整原文，CHECK/UNIQUE/NOT NULL/FK 一个都不能少，
   不要从现有 md 文档里抄——直接给 .sql 原文。
3. §5 验收矩阵是核心：一行一条规则，涉及并发/幂等/时间/外部依赖的，
   必须说明"什么条件下会红"。
4. 如果是修改已有功能：§6 影响链必须给出 文件:行；§7 不得改变的行为必须逐条列，
   这是回归测试的基底，不填等于回归测试没有依据。
5. 每条业务规则后面给出来源（文件:行）。没有出处的规则不要写。
6. 最后告诉我：这次改动你认为最容易错在哪。

不要写测试用例文档——测试用例由 TestMind 从契约自动推导。
手写用例会遮蔽它自动展开的逐字段边界，覆盖率反而降低。
```

---

## 七、开发方 AI 常犯的 5 个错

| # | 错误 | 后果 |
|---|---|---|
| 1 | 交"测试用例文档" | 无 `source` 被拒；还遮蔽自动展开的逐字段边界，覆盖率反降 |
| 2 | 从 md 里抄 DDL | `CHECK`/`UNIQUE` 抄丢 → 静态预检与 DB 对拍失效 |
| 3 | 规则不写 `file:line` | 直接拒收，白干 |
| 4 | 只报"改了什么"，不报"最不可能错在哪" | `risks` 全空 → 测试方从零猜，覆盖靠运气 |
| 5 | 改了旧功能却不填 §7 | 新功能全绿、旧功能被静默改坏，**无人发现** |

---

## 附：仓库内参考范例

| 文件 | 用途 |
|---|---|
| `examples/flash-sale/INTERFACE.md` | 契约文档的**完整实例**，§5 验收矩阵 16 条可直接看写法 |
| `examples/peak-sim/task-bundle.json` | §8 改动面的机器可读格式范例（TaskBundle v2） |
| `skills/testmind/SKILL.md` | 测试纪律四条硬规则；接入三步法 |

---

## 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-09-12 | v1.0 | 初版：流转链 / 维护规则 / 新功能与改功能两路 / 10 节骨架（§0–§9）/ 输入映射。置于 `test-mind/` 根目录随仓维护。 |
